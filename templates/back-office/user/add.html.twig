{% extends 'base-back.html.twig' %}
{% block body %}
<h2 class='center'>Ajouter un hôtelier</h2>

<br>
{{form_start(formA)}}

    
    <div class="form-group">
        <label for="">Nom</label>
        {{ form_widget (formA.nom) }}
    </div>
    <div class="form-group">
        <label for="">Prenom</label>
        {{ form_widget (formA.prenom) }}
    </div>
    
    <div class="form-group">
        <label for="">Email</label>
        {{ form_widget (formA.mail) }}
    </div>

   

    {{form_end(formA)}}


{% endblock %}