{% extends 'base-back.html.twig' %}
{% block body %}
    <h1>Modifier location </h1>

    {{form_start(formA)}}


    <div class="form-group">
        <label for="">Date arrivée</label>
        {{ form_widget (formA.datedebut) }}
    </div>
    <div class="form-group">
        <label for="">Date depart</label>
        {{ form_widget (formA.datefin) }}
    </div>
    <div class="form-group">
        <label for="">Prix </label>
        {{ form_widget (formA.prixreservation) }}
    </div>


    <button type="submit" class="btn btn-success">Modifier location</button>

    {{form_end(formA)}}


{% endblock %}